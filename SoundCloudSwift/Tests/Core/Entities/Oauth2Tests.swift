import Foundation
import Quick
import Nimble

@testable import SoundCloudSwift

class Oauth2Tests: QuickSpec {
    
    override func spec() {
        
        describe("Oauth flow") { () -> Void in
            
            it("should error if the process has already started", closure: { () -> () in
                
            })
            
            
        }
        
    }
    
}